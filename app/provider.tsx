"use client"
import React from 'react'

const provider = ({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) => {

    const CreateNewUser=()=>{
        
    }
  return (
    <div>{children}</div>
  )
}

export default provider